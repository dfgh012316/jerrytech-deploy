apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: slipkit
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/dfgh012316/jerrytech-deploy
    targetRevision: HEAD
    path: charts/app
    helm:
      valueFiles:
        - ../../apps/slipkit/values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: slipkit
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
